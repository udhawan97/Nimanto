# v0.5.3 public-surface ledger

This ledger maps the v0.5.3 hotfix claim and inherited current-surface claims to
source evidence and fail-closed acceptance checks. v0.5.2 remains immutable
historical provenance.

| Claim                                                                | Source evidence                                                                          | Public destination                   | Acceptance check                                                                                                               |
| -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ |
| Documentation and public workbench PNGs are byte-identical           | `assertScreenshotBytesMatch`; `nimanto-screenshots.json` equal asset digests             | README; website; release notes       | Negative script test rejects divergent bytes; `pnpm screenshots:check` verifies the committed pair directly                    |
| Public screenshots depict the current synthetic runtime              | Deterministic `render-screenshots.mjs` fixture and exact source/asset evidence manifest  | README; website                      | Disposable-data capture requires exact dimensions, exact source/asset digests, and bounded cross-host visual variance          |
| Escape cancels shared confirmations without committing               | Shared Escape-aware confirmation strips                                                  | README; website; release notes       | WebKit journeys cover claim rejection, draft discard, and board/table withdrawal; schedule cancellation shares `ConfirmAction` |
| Cancelled confirmations restore focus to their origin                | `ConfirmAction` focus-origin effect                                                      | README; release notes                | WebKit asserts the initiating control regains focus                                                                            |
| The mobile drawer uses one deterministic DOM-order focus policy      | `trapMobileNavigationKey`; `Workspace` drawer listener                                   | README; release notes                | Unit checks cover traversal and wrap; WebKit covers Brand, Close, section, Sign out, and wrap edges                            |
| Approved packets do not retain assurance-waiting descriptions        | Packet `approvalNeedsAssurance` condition                                                | README; release notes                | WebKit assures and approves a packet, then asserts no stale gate node or `aria-describedby`                                    |
| All public and runtime surfaces identify v0.5.3 consistently         | `validate-version-sync.mjs`; package manifests; server; providers; compose; public files | README; website; operations; notices | `pnpm version:check` plus negative script tests fail closed on every declared version seam                                     |
| The release remains source-distributed with no signed binary         | Repository release assets and distribution files                                         | README; website; release notes       | GitHub supplies tag archives; Nimanto publishes two SBOMs and their checksum manifest, with no installer                       |
| Dependency maintenance is one verified tree                          | Package manifests, lockfile, CodeQL workflow pins                                        | Release notes; notices               | Fresh SBOM identities and metadata match both inventories; frozen install, tests, full audit, licenses, CI, and CodeQL pass    |
| Release verification names exactly what the checksum manifest covers | `write-sbom-checksums.mjs`; generated v0.5.3 manifest                                    | README; website; operations; notes   | `pnpm release:check` validates both inventories and their manifest; tag archives remain GitHub-generated assets                |

## Surface inventory

- Public website: Run, Release, and Help paths; current version; exact-tag links.
- Repository: README, operations guidance, acknowledgments, notices, and release notes.
- Runtime: API and provider identities, compose image label, workspace package versions.
- Release: annotated tag, two machine-readable inventories, one exact checksum manifest, and GitHub-generated source archives.
