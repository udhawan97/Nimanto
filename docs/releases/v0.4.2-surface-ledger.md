# v0.4.2 public-surface ledger

This ledger ties each material v0.4.2 public claim to current source evidence,
the surface that publishes it, and the acceptance check that prevents drift.

| Claim                                                                                 | Source evidence                                                    | Public surface                                           | Acceptance check                                                      |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | -------------------------------------------------------- | --------------------------------------------------------------------- |
| Match publication uses one exact Profile Version and job snapshot                     | `match-publication.ts`; `saveMatch`                                | README; architecture; release notes                      | API module tests; manual and scheduled API journeys                   |
| Previewed evidence commits as one exact pending batch or not at all                   | `evidence-intake.ts`; `createEvidenceBatch`                        | README; website Method; architecture; release notes      | Preview/no-write, changed-hash, deletion and API tests                |
| Packet assurance and approval bind exact packet and manifest hashes                   | `packet-lifecycle.ts`; `saveAssurance`; `approvePacketExact`       | README; operations; architecture; release notes          | Artifact tamper, assurance history, approval journey                  |
| Action approval binds exact intent and packet; ambiguity never auto-retries           | `external-action-lifecycle.ts`; action binding columns             | README; provider boundaries; architecture; release notes | API state/runtime/outbox tests and restart recovery seam              |
| Deletion atomically captures cleanup inventory and fences later writes                | `DeletionCoordinator`; tenant-write trigger; `beginTenantDeletion` | README; operations; architecture; release notes          | Store race/fence test; resumable deletion API/WebKit journey          |
| Direct import and scheduled refresh are separate, atomic operations                   | `discovery-cycle.ts`; `MatchPublication`                           | README; architecture; release notes                      | Schedule cancellation/lease tests; provider import tests              |
| Government data is checksum-addressed, idempotent, and historically bounded           | `government-dataset.ts`; `dataset_editions`                        | README; architecture; release notes                      | Untrusted evaluation, checksum, replay and conflict API tests         |
| Application transitions use one domain policy in API and web                          | `applications.ts`; `derive.ts`; status route                       | README; release notes                                    | Domain/derive tests and board/table WebKit journey                    |
| Notices, compensation recovery, mobile navigation and public 320 px reflow are usable | `workspace.tsx`; `globals.css`; `local-beta.spec.ts`               | Website; screenshot; release notes                       | Ten sequential WebKit journeys and responsive matrix                  |
| v0.4.2 remains source-distributed with no connected-account sender or signed binary   | metadata; launcher; compose; provider adapters                     | README; website; operations; notices; release notes      | Build, links/assets, exact-SHA CI/CodeQL/Pages and release inspection |

## Surface inventory

- Narrative: `README.md`, `apps/web/app/page.tsx`, layout metadata, web manifest.
- Product visuals: `docs/assets/nimanto-landing.png`,
  `docs/assets/nimanto-workbench.png`, the website workbench copy, social card,
  icon, and animated emblem.
- Operations and architecture: `docs/operations/local-beta.md`,
  `docs/operations/provider-setup.md`, `docs/architecture/system.md`, and
  `CONTEXT.md`.
- Release and license proof: `docs/releases/v0.4.2.md`, CycloneDX/SPDX v0.4.2
  inventories, checksums, acknowledgments, and third-party notices.
- Installation: root scripts, `START-NIMANTO.command`, Dockerfile, and
  `compose.yaml` image version.

Historical v0.1.0–v0.4.1 notes, ledgers, and dependency inventories remain
unchanged as provenance records.
