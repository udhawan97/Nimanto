# v0.5.0 public-surface ledger

This ledger ties each material v0.5.0 public claim to current source evidence,
the surface that publishes it, and the acceptance check that prevents drift.

| Claim                                                                               | Source evidence                                                               | Public surface                                      | Acceptance check                                                                        |
| ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | --------------------------------------------------- | --------------------------------------------------------------------------------------- |
| Candidate Application options and decisions use one legal transition policy         | `packages/domain/src/applications.ts`; `apps/web/lib/derive.ts`; status route | README; website; architecture; release notes        | Domain/derive/API tests; board and table WebKit journey                                 |
| Consequential candidate moves require server confirmation                           | status route; `transitionCandidateApplicationStatus`                          | README; website; architecture; release notes        | API rejects an unconfirmed consequential move and accepts the confirmed retry           |
| Candidate status read-policy-write and `submittedAt` handling are atomic            | `transitionCandidateApplicationStatus`; `setApplicationStatus`                | README; architecture; release notes                 | Database transition/timestamp tests; API status journey                                 |
| Packet effects preserve candidate-recorded submission and withdrawal facts          | `applicationTransitions.packet`; `packet-lifecycle.ts`                        | README; architecture; release notes                 | Domain decision tests; lifecycle timestamp/status regression; API and WebKit journeys   |
| Five role routes share common current-record normalization                          | `packages/domain/src/roles.ts`; manual/URL routes; `discovery-cycle.ts`       | README; website Method; architecture; release notes | All-source domain test; direct and scheduled atomic-batch API tests; URL boundary tests |
| Role normalization adds no immutable history or cross-source dedupe                 | `CurrentRole`; `upsertJob`; current jobs schema                               | README; architecture; release notes                 | Architecture/doc review; schema and export tests                                        |
| Workbench mutation sequencing is UI-only and returns typed outcomes                 | `apps/web/lib/workbench-mutations.ts`; `workspace.tsx`                        | README; architecture; release notes                 | Coordinator tests; failed-save and deletion WebKit journeys                             |
| Identity credential handling remains separate from navigation and focus             | `identity-transitions.ts`; `navigation-transitions.ts`                        | README; architecture; release notes                 | Unit tests; bootstrap/invitation, keyboard, reduced-motion, and mobile WebKit checks    |
| v0.5.0 remains source-distributed with no connected-account sender or signed binary | metadata; launcher; compose; provider adapters                                | README; website; operations; notices; release notes | Build; local links/assets; exact-SHA CI/security/Pages and release inspection           |
| Published dependency inventories match the locked v0.5.0 graph                      | `pnpm-lock.yaml`; release SBOM files; checksums                               | README; acknowledgments; notices; release notes     | Frozen install, license policy, SBOM validation, published-asset checksum verification  |

## Surface inventory

- Narrative: `README.md`, `apps/web/app/page.tsx`, layout metadata, web manifest,
  and this release note.
- Product visuals: `docs/assets/nimanto-landing.png`,
  `docs/assets/nimanto-workbench.png`, the published website copy, social card,
  icon, and animated emblem.
- Operations and architecture: `docs/operations/local-beta.md`,
  `docs/operations/provider-setup.md`, `docs/architecture/system.md`, and
  `CONTEXT.md`.
- Release and license proof: `docs/releases/v0.5.0.md`, CycloneDX/SPDX v0.5.0
  inventories, checksums, acknowledgments, and third-party notices.
- Installation: package scripts, `START-NIMANTO.command`, Dockerfile, and
  `compose.yaml` image version.

Historical v0.1.0–v0.4.2 notes, ledgers, and dependency inventories remain
unchanged as provenance records. Historical planning documents remain proposals;
`docs/architecture/system.md` records the implemented current model.
