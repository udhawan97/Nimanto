# v0.5.5 public-surface ledger

This ledger maps the v0.5.5 maintenance claims to implementation evidence and
fail-closed acceptance checks. Earlier release records remain immutable
historical provenance.

| Claim                                                        | Source evidence                                                              | Public destination                            | Acceptance check                                                                                                                                       |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| The API's locked production graph uses Fastify 5.12.1        | `apps/api/package.json`; `pnpm-lock.yaml`; upstream v5.12.1 security release | README; release notes; notices                | Frozen install, API/package tests, audit, production license inventory, fresh SBOM comparison, CI, container build, and CodeQL pass                    |
| v0.5.5 adds no Nimanto feature or database migration         | Dependency-only application diff; unchanged Nimanto product/database source  | Release notes; operations                     | Diff review plus the complete package, API, database, and WebKit suites found no regression in the supported v0.5.4 workflows and schema-v4 boundaries |
| Public screenshots depict the exact v0.5.5 synthetic runtime | Build-first deterministic screenshot fixture and source/asset manifest       | README; website                               | `pnpm screenshots:render` builds before updating; `pnpm screenshots:check` verifies dimensions, digests, byte parity, and variance                     |
| Every current public and runtime surface identifies v0.5.5   | Version validator; manifests; API/provider identities; compose; website      | README; website; operations; notices          | `pnpm version:check` and negative script tests cover every declared version seam                                                                       |
| Release inventories describe the locked v0.5.5 source tree   | CycloneDX/SPDX generation, sanitizer, freshness check, checksum writer       | Release notes; acknowledgments; GitHub assets | `pnpm sbom:check` and `pnpm release:check`; downloaded assets must pass the published SHA-256 manifest                                                 |
| Distribution remains source-only                             | Repository distribution files and release asset set                          | README; website; release notes                | GitHub supplies tag archives; Nimanto publishes two SBOMs and one checksum manifest, with no installer                                                 |

## Surface inventory

- Public website: current version, exact-tag release links, runtime choices,
  current screenshot, trust boundary, and help paths.
- Repository: README, architecture, operations, provider guidance,
  acknowledgments, notices, release notes, and this ledger.
- Runtime: API and provider identities, compose image label, workspace package
  versions, and the unchanged v0.5.4 product behavior on schema version 4.
- Release: annotated tag, two machine-readable inventories, one exact checksum
  manifest, GitHub-generated source archives, CI, CodeQL, container, and Pages.
