# v0.5.1 public-surface ledger

This ledger ties each material v0.5.1 public claim to current source evidence,
the surface that publishes it, and the acceptance check that prevents drift.
v0.5.0 claims that this release does not change remain governed by the
[v0.5.0 ledger](v0.5.0-surface-ledger.md).

| Claim                                                                          | Source evidence                                                               | Public surface                             | Acceptance check                                                                        |
| ------------------------------------------------------------------------------ | ----------------------------------------------------------------------------- | ------------------------------------------ | --------------------------------------------------------------------------------------- |
| Both entry routes state the launch-key prerequisite when they lack it          | `WorkspaceStart` `needsLaunchKey`, `launchKeyNoteId`                          | README; release notes; operations          | WebKit journey asserts a shared `aria-describedby` naming `.nimanto-data/launch-secret` |
| Consequential candidate decisions are confirmed in-product, not by the browser | `ConfirmAction`; `requestMove`; `confirmationPrompt`                          | README; release notes                      | Two rewritten and two new WebKit journeys click real confirm and cancel controls        |
| Rejecting a claim asks once and states that the state is final                 | `ConfirmAction` on the claim row; `rejectEvidence` `WHERE status = 'pending'` | README; release notes                      | WebKit journey arms, cancels, re-arms, confirms, and asserts the final-state copy       |
| A successful action returns keyboard focus to its origin                       | `createWorkbenchMutations` `captureFocus`/`restoreFocus`; `focusOrigin`       | README; release notes                      | Three coordinator unit tests; runtime check on Explain fit, Track, and Generate         |
| The application pipeline exposes the stages it cannot fit                      | `useOverflowFlag`; `.board[data-overflowing]`; `move` `scrollIntoView`        | README; release notes                      | WebKit journey asserts region role, tabindex, overflow flag, and post-move visibility   |
| A gated Approve names the assurance it is waiting for                          | packet action row `aria-describedby`; `approve-gate-<id>` note                | Release notes                              | WebKit journey reads the description off the disabled control                           |
| Applications never offers to track "another" role when none are tracked        | `dashboard.applications.length` header label                                  | Release notes                              | WebKit journey asserts the header and empty-state labels agree                          |
| Quick navigation announces the destination its highlight is on                 | `command-palette.tsx` combobox wiring and option ids                          | Release notes                              | Component test asserts `aria-activedescendant` tracks arrow-key movement                |
| Section focus never takes focus off work already underway                      | `focusSectionBelowHeader` active-element guard                                | Release notes                              | Two unit tests; deletion receipt journey passes ten runs in ten                         |
| Throttling reports as throttling, not as a failed local service                | `setErrorHandler` framework-status pass-through; `RATE_LIMITED` copy          | README; release notes; operations          | API test bounded above the configured ceiling asserts 429, code, and `Retry-After`      |
| v0.5.1 changes no schema, domain policy, or product boundary                   | schema version 3; `packages/domain` unchanged; provider adapters unchanged    | README; release notes; operations; notices | Full `pnpm check`; unchanged domain and database suites; architecture review            |
| Published dependency inventories match the locked v0.5.1 graph                 | `pnpm-lock.yaml`; v0.5.1 SBOM files; checksums                                | README; acknowledgments; notices           | Frozen install, license policy, SBOM validation, checksum verification                  |

## Surface inventory

- Narrative: `README.md`, `apps/web/app/page.tsx`, layout metadata, web manifest,
  and this release note.
- Product visuals: unchanged from v0.5.0. `docs/assets/nimanto-landing.png` and
  `docs/assets/nimanto-workbench.png` predate the entry-screen and pipeline
  changes and are not re-shot for this release; they remain accurate for the
  surfaces they depict.
- Operations and architecture: `docs/operations/local-beta.md`,
  `docs/operations/provider-setup.md`, `docs/architecture/system.md`, and
  `CONTEXT.md`.
- Release and license proof: `docs/releases/v0.5.1.md`, CycloneDX/SPDX v0.5.1
  inventories, checksums, acknowledgments, and third-party notices.
- Installation: package scripts, `START-NIMANTO.command`, Dockerfile, and
  `compose.yaml` image version.

Historical v0.1.0–v0.5.0 notes, ledgers, and dependency inventories remain
unchanged as provenance records.
