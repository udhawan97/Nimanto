# v0.4.0 public-surface ledger

This ledger ties each material v0.4.0 public claim to current source evidence,
the surface that publishes it, and the acceptance check that prevents drift.

| Claim                                                                                             | Source evidence                                                     | Public surface                                          | Acceptance check                                                                     |
| ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| Retained profile and match history is tenant-scoped, bounded, and on demand                       | `packages/database/src/store.ts`; `/v1/history/*`; `StoredHistory`  | README; website Method; architecture; release notes     | Store foreign-cursor test; API tenant-boundary test; WebKit retained-history journey |
| Profile and same-role match comparisons are literal, not causal or replay proof                   | `profileVersionDiff`; `StoredHistory` boundary copy                 | README; screenshot; operations guide; release notes     | Derivation unit test; WebKit same/reverse selection and value assertions             |
| Packet generations are history, not lineage; assurance exposes packet-local ordinals only         | `listApplicationPackets`; `listAssuranceRuns`; `PacketHistoryPanel` | README; architecture; operations guide; release notes   | Store ordering/sequence test; API response test; WebKit delta/history journey        |
| Record review begins at 336 elapsed hours and infers no outcome                                   | `recordReviewQueue`; `nextSteps`                                    | README; website Method/Boundary; release notes          | Unit boundary/due-time test; WebKit empty-state and rendering assertion              |
| Application cohorts are explicit creation-time raw counts using current snapshot filters          | `applicationCohortCounts`; `Applications`                           | README; operations guide; release notes                 | Derivation unit tests for unmatched/unknown; WebKit 320 px cohort assertion          |
| Export v2 is sensitive inspection JSON, not restore or immutable history                          | `exportTenant`; `/v1/export`; `DataControls`                        | README; architecture; operations guide; release notes   | Store/API export tests; WebKit confirmation gate                                     |
| v0.4.0 remains a source-distributed local beta with no connected-account sender or native release | API metadata; provider adapters; package scripts; workflows         | README; website; provider guide; notices; release notes | Build; API tests; source/release asset inspection; served-site verification          |

## Surface inventory

- Narrative: `README.md`, `apps/web/app/page.tsx`, metadata, web manifest.
- Product visuals: `docs/assets/nimanto-landing.png`,
  `docs/assets/nimanto-workbench.png`, social card, icon, animated emblem.
- Operations and architecture: `docs/operations/local-beta.md`,
  `docs/operations/provider-setup.md`, `docs/architecture/system.md`.
- Release and license proof: `docs/releases/v0.4.0.md`, CycloneDX/SPDX
  v0.4.0 inventories, acknowledgments, third-party notices.
- Installation: root scripts, `START-NIMANTO.command`, Dockerfile, and
  `compose.yaml` image version.

Historical v0.1.0–v0.3.0 release notes and dependency inventories remain
unchanged as provenance records.
