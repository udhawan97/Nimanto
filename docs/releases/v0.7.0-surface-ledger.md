# v0.7.0 public-surface ledger

This ledger maps the v0.7.0 architecture claims to implementation evidence and
fail-closed acceptance checks. Earlier release records remain immutable
historical provenance.

| Claim                                                               | Source evidence                                                                                            | Public destination                  | Acceptance check                                                                                                                   |
| ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Applications has one identity-bounded working-state module          | `applications-workbench` reducer; workspace identity/deletion resets; retained mutation coordinator        | README; architecture; release notes | Reducer and WebKit tests prove section retention, identity clearing, explicit discard, and exact-submission clearing               |
| Dashboard assembly observes one coherent database snapshot          | `DashboardRead`; concrete `NimantoStore.transaction`; persistence-owned latest-selection queries           | Architecture; release notes         | An interleaving test proves a concurrent write waits for the complete read transaction; API tests cover Dashboard assembly         |
| The Dashboard `personalFunnel` is counts-only                       | Outcome aggregation in `DashboardRead`; literal outcome histories retained on Application records          | README; architecture; release notes | API contract asserts the exact aggregate and exact literal outcome fields; suites reject employer-screening or probability claims  |
| One pure policy owns application follow-up behavior                 | `applicationFollowUpPolicy`; API parser; transactional store change; workbench due derivation              | README; architecture; release notes | Domain, database, API, derivation, and WebKit tests cover strict dates, withdrawn inactivity, local due days, and unchanged status |
| Schema changes run as ordered, recorded, transactional migrations   | `migrateDatabase`; schema steps 1–6; transactional version insertion and version-6 hash backfill           | Operations; architecture; notes     | Database tests cover fresh creation, historical upgrades, interrupted rollback/resume, and fail-closed future schema versions      |
| v0.7.0 adds no provider, notification, or external-action authority | Follow-up policy has no effects; Dashboard is read-only; existing Packet and Action Intent gates unchanged | README; release notes; providers    | Full unit/integration/WebKit suites, council review, provider-boundary tests, and release-gate verification                        |
| Release inventories describe the locked v0.7.0 source tree          | CycloneDX/SPDX generation, sanitizer, freshness check, checksum writer                                     | Notes; acknowledgments; assets      | `pnpm sbom:check`, `pnpm release:check`, and independent verification of downloaded assets against the SHA-256 manifest            |

## Surface inventory

- Public website: current version, exact-tag release links, current screenshots,
  trust boundaries, and help paths.
- Repository: README, architecture, operations, provider guidance,
  acknowledgments, notices, release notes, and this ledger.
- Runtime: Applications state reducer, coherent Dashboard read, shared follow-up
  policy, ordered schema migrations, existing API/provider identities, and
  compose image label.
- Release: annotated tag, two machine-readable inventories, one exact checksum
  manifest, GitHub-generated source archives, CI, CodeQL, container, and Pages.
