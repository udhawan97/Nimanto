# v0.6.0 public-surface ledger

This ledger maps the v0.6.0 claims to implementation evidence and fail-closed
acceptance checks. Earlier release records remain immutable historical
provenance.

| Claim                                                                  | Source evidence                                                                                                     | Public destination                       | Acceptance check                                                                                                                                            |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A candidate can set, change, and clear one follow-up date              | Schema v5; tenant-scoped store method; `PUT /v1/applications/:id/follow-up`; board/table editor                     | README; website; release notes           | Database and API tests prove tenant isolation, set/clear behavior, and unchanged application status; WebKit proves the complete control journey             |
| Only a real date-only value or `null` is accepted                      | API date parser and endpoint contract tests                                                                         | Release notes; operations                | Tests reject year zero, ambiguous US-style dates, impossible calendar dates, timestamps, and empty strings                                                  |
| The date controls Review due without inventing employer state          | `followUpNote`; `recordReviewQueue`; explicit `candidate_reminder` and `record_activity` bases                      | Workbench; README; operations            | Derivation tests prove future suppression, due inclusion, fallback restoration, withdrawn inactivity, and basis-specific queue summaries                    |
| Withdrawal retains a date without leaving it active                    | Transactional status gate; inactive board/table note; withdrawn editor permits clear only                           | Workbench; README; release notes         | Store, API, derivation, and WebKit tests prove retention, rejected changes, queue exclusion, clear access, and reactivation behavior                        |
| Unsaved follow-up work remains local to the current identity-bound tab | Lifted reminder draft state, exact-snapshot commit, identity-transition clearing                                    | Workbench; README                        | WebKit proves section-navigation retention without a network write, withdrawal reset, save/clear focus recovery, and explicit revoked-session draft removal |
| Both application views expose the same saved state                     | Shared derivation rendered in board cards and table identity rows; responsive form rules                            | Workbench screenshot and runtime         | WebKit asserts matching due copy in both views and no editor or document overflow at 320, 375, and 1280 pixels                                              |
| v0.6.0 adds no notification, provider, or external-action authority    | No reminder worker/provider/sender; endpoint only writes `follow_up_on`; existing action approval and runtime gates | README; release notes; provider guidance | Diff review, full unit/integration/WebKit suites, council review, network/provider boundary tests, and release-gate verification                            |
| Release inventories describe the locked v0.6.0 source tree             | CycloneDX/SPDX generation, sanitizer, freshness check, checksum writer                                              | Release notes; acknowledgments; assets   | `pnpm sbom:check` and `pnpm release:check`; downloaded assets must pass the published SHA-256 manifest                                                      |

## Surface inventory

- Public website: current version, exact-tag release links, current screenshot,
  trust boundaries, and help paths.
- Repository: README, architecture, operations, provider guidance,
  acknowledgments, notices, release notes, and this ledger.
- Runtime: strict application follow-up endpoint, additive schema version 5,
  board/table controls, due queue, API/provider identities, and compose image
  label.
- Release: annotated tag, two machine-readable inventories, one exact checksum
  manifest, GitHub-generated source archives, CI, CodeQL, container, and Pages.
